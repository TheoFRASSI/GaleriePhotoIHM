#pragma once

#include <QWidget>

#include "image.h"
#include "smartdelete.h"

class ListeImages : public QWidget
{
    Q_OBJECT
public:
    explicit ListeImages(QWidget *parent = nullptr);


signals:

public slots:
};
